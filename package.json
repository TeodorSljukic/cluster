{
  "name": "cluster",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd nextjs && npm install && npm run build",
    "start": "cd nextjs && npm start",
    "dev": "cd nextjs && npm run dev"
  }
}
